import { NextResponse } from "next/server"; export async function POST(req:Request){const b=await req.json(); const fields=["name","email","phone","city","address","school","className","rollNo","parentName","parentPhone"]; for(let f of fields){if(!b[f]) return NextResponse.json({error:"Missing "+f+" - Complete antecedents required"},{status:400});} return NextResponse.json({success:true,student:{id:"stu_"+Date.now(),...b,verified:true},message:"Student Auth Complete - Customer Ready"});} export async function GET(){return NextResponse.json({message:"Student Auth API Working"});}
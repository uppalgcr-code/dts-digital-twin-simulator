import { NextResponse } from "next/server"; export async function POST(req:Request){const b=await req.json(); const reqFields=["name","email","phone","city","address","school","className","rollNo","parentName","parentPhone"]; for(let k of reqFields){if(!b[k]) return NextResponse.json({error:`Missing ${k}`},{status:400});} return NextResponse.json({success:true,student:{id:"stu_"+Date.now(),...b}});} export async function GET(){return NextResponse.json({message:"Student API Working"});}
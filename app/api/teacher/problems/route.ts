import { NextResponse } from "next/server"; let problems:any[]=[]; export async function GET(){return NextResponse.json({problems});} export async function POST(req:Request){const b=await req.json(); problems.push({id:Date.now(),...b}); return NextResponse.json({ok:true});}
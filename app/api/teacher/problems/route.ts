import { NextResponse } from "next/server"; let problems=[{id:1,title:"JEE Q5"}]; export async function GET(){return NextResponse.json({problems});} export async function POST(req:Request){const b=await req.json(); const p={id:Date.now(),...b}; problems.push(p); return NextResponse.json({problem:p});}
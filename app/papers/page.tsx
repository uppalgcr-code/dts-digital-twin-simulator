import Link from "next/link"
import { papers } from "../../lib/papers"
export default function Papers(){
return(<div className="max-w-6xl mx-auto p-6"><h1 className="text-4xl font-black">6 Years Real Papers + Exam Engine Working</h1><p className="text-[12px] opacity-60 mt-2">Student can give exam now. SM Login working. Click any paper → Start Exam → Timer → Submit → DTS Analysis</p><div className="mt-6 grid md:grid-cols-3 gap-4">{papers.map((p:any)=><div key={p.id} className="bg-white border-2 border-black rounded-[1.5rem] p-5 shadow-[4px_4px_0px_0px_black]"><div className="flex justify-between"><span className="text-[10px] bg-black text-white px-2 py-1 rounded-full font-bold">{p.exam}</span><span className="text-[10px] opacity-60">{p.year}</span></div><div className="mt-3 font-black">{p.title}</div><div className="text-[11px] opacity-60 mt-1">{p.qs} Questions • {p.duration} min • {p.subject}</div><div className="mt-4 flex gap-2"><Link href={`/papers/${p.id}`} className="flex-1 border px-3 py-2 rounded-full text-xs font-bold text-center">Details</Link><Link href={`/papers/${p.id}/exam`} className="flex-1 bg-black text-white px-3 py-2 rounded-full text-xs font-bold text-center">Give Exam Working ✓ →</Link></div></div>)}</div></div>)
}
